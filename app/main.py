"""FastAPI entrypoint for the IncidentForge application."""

from fastapi import FastAPI

# TODO: Configure the application, include routers, and add startup/shutdown hooks.
app = FastAPI(title="IncidentForge")
